#import "../fragments/fragment-task-view.graphql"

subscription taskInDocument($documentId: DocumentId!) {
    taskInDocument(documentId: $documentId) {
        ...TaskListItemInterfaceFragment
    }
}
