#import "../fragments/fragment-resolution.graphql"

subscription resolutionInDocument($documentId: DocumentId!) {
    resolutionInDocument(documentId: $documentId) {
        ...resolutionFragment
    }
}
