mutation outgoingDocPreRegisterAndSend(
    $documentId: DocumentId!
    $prefix: String!
) {
    outgoingDocMutationAPI {
        registerReservation(documentId: $documentId, prefix: $prefix)
    }
}
