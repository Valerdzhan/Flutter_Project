#import "../fragments/fragment-task-resolution.graphql"

mutation editResolutionTask($input: EditTaskMutationInput!) {
      editResolutionTask(input: $input) {
            ...taskResolutionFragment 
      }
    }