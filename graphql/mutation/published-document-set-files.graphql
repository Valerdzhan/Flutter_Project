mutation PublishedDocumentSetFiles(
    $documentId: DocumentId!
    $files: [String!]!
) {
    documentMutationAPI(documentId: $documentId) {
        publishedDocumentSetFiles(files: $files)
    }
}
