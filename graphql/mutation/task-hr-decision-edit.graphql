#import "../fragments/fragment-task-hr-decision.graphql"

mutation editDecimalTask($input: EditInternalTaskMutationInput!) {
    editForHRDecisionTask(input: $input) {
        ...decisionTaskFragment
    }
}
