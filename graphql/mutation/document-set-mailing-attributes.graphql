mutation SetMailingAttributes(
    $documentId: DocumentId!
    $users: [UserId!]!
    $groups: [MailingGroupId!]!
) {
    documentMutationAPI(documentId: $documentId) {
        setMailingAttributes(users: $users, groups: $groups)
    }
}
