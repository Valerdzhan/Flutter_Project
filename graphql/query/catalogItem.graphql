#import '../fragments/fragment-reference-value.graphql'

query catalogItem($key: String!) {
  catalogItem(key: $key) {
    key
    title
    isUsed
    values {
      ...referenceValueFragment
    }
  }
}