#import "../../fragments/fragment-attachments.graphql"

query unlockedAttachments($documentId: DocumentId!) {
    documentQueryAPI(documentId: $documentId) {
        unlockedAttachments {
            ...attachmentsFragment
        }
    }
}
